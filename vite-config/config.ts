import type { UserConfig } from "vite";

/**
 * User configuration for Vite
 * @description 这里用户可自定义配置，如果用户自定义配置，则会完全覆盖框架中已经内置的配置.
 */
const userConfig: UserConfig | undefined = undefined;

export default userConfig;